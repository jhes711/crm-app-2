{
  "name": "crm-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "npm install && cd ../frontend && npm install && npm run build",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "nodemailer": "^6.9.14",
    "resend": "^6.12.4"
  }
}
